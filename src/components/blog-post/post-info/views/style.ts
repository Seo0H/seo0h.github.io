import { motion } from 'framer-motion';
import styled from 'styled-components';

export const DotWrapper = styled(motion.div)`
  padding: 0.1rem;
`;

export const CationIconWrapper = styled(motion.div)`
  width: 100%;
  display: flex;
  justify-content: center;
  align-items: center;
  margin-bottom: 0.2rem;
`;
